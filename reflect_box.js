javascript:(function() {     var detailsHTML = `  <div class="alert alert-info">  <h5>  Reflect </h5><p>Think about your response, then click the feedback link below for more information</p></div><p>Question goes here</p><textarea name="message" rows=" 5 " cols=" 45 " placeholder=" Type your answer in this space (It will not be saved or seen by anyone else)"></textarea><details>  <summary style="border: solid 2px  lightgray; padding:15px; margin:3px; border-radius:7px">Feedback</summary><p>Feedback here:</p></details><br><br> `;      function pasteHtmlAtCaret(html) {         var sel, range;         if (window.getSelection) {             sel = window.getSelection();             if (sel.getRangeAt && sel.rangeCount) {                 range = sel.getRangeAt(0);                 range.deleteContents();                  var el = document.createElement("div");                 el.innerHTML = html;                 var frag = document.createDocumentFragment(), node, lastNode;                 while ((node = el.firstChild)) {                     lastNode = frag.appendChild(node);                 }                 range.insertNode(frag);                  if (lastNode) {                     range = range.cloneRange();                     range.setStartAfter(lastNode);                     range.collapse(true);                     sel.removeAllRanges();                     sel.addRange(range);                 }             }         } else if (document.selection && document.selection.type != "Control") {             document.selection.createRange().pasteHTML(html);         }     }      pasteHtmlAtCaret(detailsHTML);  })();
